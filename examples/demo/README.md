# Demo output (simulated)

These files were produced by `examples/run_demo.sh` against `llmeval.fakeserver`, a timing model
with configurable latency. **They demonstrate the report format, not any GPU's performance.**
Per-token times sit near 13 ms rather than the configured 10 ms because of Python `sleep` overhead.
